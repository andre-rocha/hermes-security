# Projeto Hermes Security
Criado para fins acadêmicos

## Setup
virtualenv -p python3 env
ln -s env/bin/activate
. activate
sudo apt-get install redis-server
pip install -r requirements.txt

## Iniciar
Linux ONLY :
	./run.sh
